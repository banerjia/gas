<% content_for :head do %>
	<%= stylesheet_link_tag "table"%>
	<style type="text/css" media="screen">
		div#page_body {text-align:center;}
		div#sub_container {
			margin: 0 auto;
			text-align: left;
			width:70%;
		}
	</style>
<% end %>
<% content_for :left_column do %>

<% end %>
<%= render :partial => "stores/stores", :locals => {:store_collection =>  stores, :ajax_path => company_stores_in_state_path(stores[0][:company_id],stores[0][:state_code])}%>