<% content_for :left_column do %>
	<h2>Options</h2>
	<%- if defined?(search_params) -%>
	<p><%= link_to "All Orders from Today", orders_dashboard_path -%></p>
	<%- end -%>
	
	<%- 
		if defined?(facets) 
			path_prefix = orders_path + '?es=1&' 
			path_prefix += search_params.to_param if search_params
			
			if facets['states'] 
	-%>
	<h3>Filter by Shipping State</h3>
	<ul class="nav_items">
	<%-
				facets['states'].each do |facet_term|
					states_path = path_prefix +"&shipping_state=#{facet_term['term']}"
	-%>
					<li <%= search_params[:shipping_state] ==  facet_term['term'] ? raw("class='current'"): "" -%>><%= link_to facet_term[:state_name], states_path -%></li>
	<%-			end
			end
	-%>		
	</ul>
	<%-		if facets['chains'] -%>
	<h3>Filter by Chain</h3>
	<ul class="nav_items">
	<%-
				facets['chains'].each do |facet_term|
					chains_path = path_prefix +"&company_id=#{facet_term['term']}"
	-%>
					<li <%= search_params[:company_id].to_i ==  facet_term['term'].to_i ? raw("class='current'"): "" -%>><%= link_to_if search_params[:company_id].to_i !=  facet_term['term'].to_i,  facet_term[:company_name], chains_path -%></li>
	<%-			end
			end
	-%>		
	</ul>
	<%- end -%>
<% end %>
<% content_for :head do -%>
	<style type="text/css" media="screen">
		page_body#h3 {font-size: 10pt}
		page_body ul.nav_items li.current {
			background-color: #ddd;
			color: #000;
			font-weight: bold;
		}
	</style>
<% end -%>
<%= render :partial => 'order_listing', :locals => {:orders => orders, :show_store => true, :more_pages => more_pages, :search_title => search_title} %>
